# ============================================
# Environment Variables for Docker Compose
# ============================================
# Copy this file to .env or reference it in docker-compose.yml
#
# IMPORTANT: Change JWT_SECRET before deploying to production!

PORT=5000
MONGO_DB_URI=mongodb://chat-mongodb:27017/chatapp
JWT_SECRET=change-me-in-production
NODE_ENV=production
